export const getResultsObjectForTable = (gazName, text, actualState, color) => {
  return {
    gazName: gazName,
    text: text,
    actualState: actualState,
    color: color,
    types: [],
  };
};
