export const getPossibleMatchingsTypes = () => {
  return {
    all: 'all types',
    'ref (from live data)': 'live references',
    ref: 'reference table',
    match: 'matchings',
    match_linked_with_ref: 'matchings linked with references',
  };
};
